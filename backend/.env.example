DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ayan_taraz?schema=public"
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
JWT_SECRET=change-me-64-char-random-string-abcdefghijklmnop
JWT_EXPIRES_IN=86400
CORS_ORIGINS=http://localhost:3000
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000

# SMS Configuration — see docs/SMS_SETUP.md for full guide
# If SMS_API_URL is empty, OTP codes are logged to console (dev mode only)
# In production, set both URL and KEY to your SMS gateway/middleware
SMS_API_URL=https://your-sms-provider.com/api/send
SMS_API_KEY=
