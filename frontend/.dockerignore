# Don't copy these into the Docker build context — speeds up builds
# and prevents stale local node_modules / .next from leaking into the image.
node_modules
npm-debug.log
*.log
.env
.env.local
.env.*.local
.env.example
.next
.git
.gitignore
.gitattributes
.vscode
.idea
coverage
*.md
Dockerfile
Dockerfile*
.dockerignore
docker-compose*.yml
eslint*
